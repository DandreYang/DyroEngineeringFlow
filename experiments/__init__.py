"""First-party optional surfaces shipped with the ``dyro`` wheel.

These modules are installable (``import experiments…``, ``dyro dispatch …``) but
remain optional relative to Core delivery control: they do not replace gates,
review, signoff, or merge. See ADRs under ``docs/adr/``.
"""
